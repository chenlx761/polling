<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.Polling" parent="android:Theme.Material.Light.NoActionBar" />

    <style name="MyAppTheme" parent="Theme.AppCompat.Light.DarkActionBar">

        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorAccent">@color/primary</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>


    </style>

    <style name="LoginAppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- 基础颜色配置 -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorAccent">@color/primary</item>

        <!-- 移除标题栏 -->
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>

        <!-- 沉浸式配置 -->
        <item name="android:windowTranslucentStatus">true</item> <!-- 开启透明状态栏 -->
        <item name="android:windowTranslucentNavigation">false</item> <!-- 导航栏通常保持默认，除非你也需要透明导航栏 -->

        <!-- Android 6.0+ 状态栏图标颜色适配（如果是浅色背景，让图标变成深色） -->
        <item name="android:windowLightStatusBar">true</item>
    </style>
    <style name="MainAppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- 基础颜色配置 -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorAccent">@color/primary</item>

        <!-- 移除标题栏 -->
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>

        <!-- 沉浸式配置 -->
        <item name="android:windowTranslucentStatus">true</item> <!-- 开启透明状态栏 -->
        <item name="android:windowTranslucentNavigation">false</item> <!-- 导航栏通常保持默认，除非你也需要透明导航栏 -->

        <!-- Android 6.0+ 状态栏图标颜色适配（如果是浅色背景，让图标变成深色） -->
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <!-- 登录按钮样式 -->
    <style name="LoginButton" parent="Widget.MaterialComponents.Button">
        <item name="android:textColor">@color/white</item>
        <item name="backgroundTint">@color/primary</item>
        <item name="cornerRadius">12dp</item>
        <item name="android:textSize">16sp</item>
        <item name="android:textAllCaps">false</item>
    </style>

    <!-- TabLayout文字样式 -->
    <style name="TabTextAppearance" parent="TextAppearance.Design.Tab">
        <item name="android:textSize">14sp</item>
        <item name="textAllCaps">false</item>
    </style>

    <!-- 图片预览全屏主题 -->
    <style name="ImagePreviewTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowBackground">@color/black</item>
    </style>
</resources>